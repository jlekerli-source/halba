An agent says the work shipped. But the machine receipt says the proof was recorded. Done is a claim.

Halba turns reports, diffs, source files, and run receipts into one bounded evidence packet.

Run the packet through Proof Mode. GPT-5.6 Sol extracts atomic claims and precise citations.

Then deterministic guards check the actual bytes. When model confidence meets a contradictory receipt, the receipt wins.

Open the real patch, exact line range, and hash together. Halba shows what changed, what is verified, and what is stale or unsupported.

Only uncertainty reaches the human gate. Approve, reject, or resolve the reasoning boundary locally.

The public regression corpus passes nine of nine cases, with exact grounding and zero false positives.

Halba. Agent claims in. Traceable proof out.
