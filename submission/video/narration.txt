Agents compress hours of work into one confident word: done. Halba treats that as a claim, not a fact.

Trust Inbox turns risky completion claims across local workspaces into one deterministic operations queue.

In this public-safe benchmark, 120 runs become 11 attention items. Declared criticality and inspectable policy reasons put the critical contradiction first.

Open the source-backed packet. When model confidence meets a contradictory machine receipt, the receipt wins. Model prose has zero authority.

Inspect the exact lines and hashes together. Halba shows what changed, what is verified, and what is stale, missing, or unsupported.

A human can approve, reject, resolve, or request proof. The decision stays scoped to the exact evidence identity.

The deterministic benchmark finds all 11 gold attention items across three workspaces, with precision and recall of one.

Codex accelerated Halba from audit to ship: building Trust Inbox, Proof Mode, and the evaluation corpus. It also produced the release proof and this film. Inside Halba, GPT-5.6 makes testable, cited claims. Deterministic guards remain authoritative.

Halba. Language proposes. Evidence decides. The human remains accountable.
