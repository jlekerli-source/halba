# Demo film evidence

`halba-demo.mp4` is the accepted 72-second Halba Build Week product film. It is rendered from `submission/video/` using the public-safe screenshots in `artifacts/screenshots/`.

- SHA-256: `bedaad04931a630fced3555aaf32cc3a9d499015306890b0defab6ef0c6779fa`
- Container: MP4
- Video: H.264, 1280 × 720, 30 fps
- Audio: AAC, 48 kHz stereo
- Loudness inspection: −16.3 LUFS integrated, −4.3 dB true peak
- Runtime: 72.04 seconds encoded

`halba-demo-still.png` is the accepted poster. `narration.m4a`, `sound-bed.wav`, `proof-pulse.wav`, `halba-icon.svg`, and the checked-in Remotion source make the render reproducible. Narration uses HeyGen's authenticated Daniel voice, generated and aligned per scene; the sound bed and transition pulse were synthesized locally. No third-party music is included.

`devpost-thumbnail.png` is the 1200 × 800 gallery key art and follows the film's Claim → Source → Guard → Human language. `halba-og-card.png` is the 1200 × 630 link-preview treatment, and `halba-social-square.png` is the 1080 × 1080 social crop; those cards follow the product's broader Claim → Evidence → Guard → Human language. In both treatments, source evidence is the bounded evidence step rather than a separate authority layer.
