# Reviewed change summary

Review gate staleness now accepts an explicit evaluation time.
Production callers still default to the actual current time.
The public runtime defaults to checked-in demo data and a contained source root.
